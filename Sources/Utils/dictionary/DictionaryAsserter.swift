import Foundation

class DictionaryAsserter {
    /**
     * Asserts if PARAM: dict has PARAM: key
     */
    static func hasKey<Key, Value>(_ dict:[Key: Value], _ key:Key) -> Bool{
        return dict[key] != nil
    }
    static func hasKeys<Key, Value>(_ dict:[Key: Value], _ keys:[Key]) -> Bool{
        for myKey  in toppings.keys{
            toppings[myKey] = toppings[myKey]! * 1.10
        }
        
        dict.
        //return dict[key] != nil
    }
}
