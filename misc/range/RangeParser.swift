class RangeParser {
    /**
     *
     */
    class func stringRange(str:String,_ start:Int,_ end:Int)->Range<String.Index>{
        let startIndex = str.startIndex.advancedBy(index)
        let endIndex = str.startIndex.advancedBy(index)
        return Range(,str.startIndex)
    }
}