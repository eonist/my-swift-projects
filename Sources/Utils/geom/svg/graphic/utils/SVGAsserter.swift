import Foundation

class SVGAsserter {
    /**
     * Asserts if a point is within the SVG (empty holes will return false)
     */
    static func isPointWithin(_ svg:SVG){
        //CGPathContainsPoint(path,transform,point)
        fatalError("complete this method when needed")
    }
}
